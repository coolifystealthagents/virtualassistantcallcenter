import {blogPosts} from '../../../data';import {BlogListing,PAGE_SIZE} from '../../blog-listing';export function generateStaticParams(){return Array.from({length:Math.max(1,Math.ceil(blogPosts.length/PAGE_SIZE))},(_,i)=>({page:String(i+1)}))}export async function generateMetadata({params}:{params:Promise<{page:string}>}){const {page}=await params;return {title:`Blog - Page ${page}`,description:'Guides for planning Philippines-based staffing.',alternates:{canonical:`/blog/page/${page}`},openGraph:{title:`Blog - Page ${page}`,description:'Guides for planning Philippines-based staffing.',url:`/blog/page/${page}`,type:'website'}}}export default async function NumberedBlogPage({params}:{params:Promise<{page:string}>}){const {page}=await params;const n=Number(page);return <BlogListing page={n}/>}
