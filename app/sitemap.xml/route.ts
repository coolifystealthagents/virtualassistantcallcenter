const aug19Paths=Object.entries(aug19Meta).map(([slug,value])=>`/${(value as {family:string}).family}/${slug}`);import aug19Meta from '../aug19-meta.json';import {fleetServices} from '../fleet-content';import {blogPosts} from '../data';import {getContent} from '../content';const base='https://virtualassistantcallcenter.com';export async function GET(){const pages=['','/services','/pricing','/workflows','/qa-scorecard','/blog','/blog/top-25-virtual-assistant-call-center-companies','/research','/alternatives','/contact','/privacy','/terms','/cancellation-policy'];const fileBlogs=getContent('blog');const research=getContent('research');const blogPages=Array.from({length:Math.max(1,Math.ceil((blogPosts.length+fileBlogs.length)/20))},(_,i)=>i+1).filter(n=>n>1).map(n=>`/blog/page/${n}`);const alternatives=getContent('alternatives').map(p=>`/alternatives/${p.slug}`);const urls=[...aug19Paths,...pages,...fleetServices.map(s=>`/services/${s.slug}`),...blogPosts.map(p=>`/blog/${p.slug}`),...fileBlogs.map(p=>`/blog/${p.slug}`),...blogPages,...research.map(p=>`/research/${p.slug}`),...alternatives];const xml=`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${Array.from(new Set(urls)).map(x=>`<url><loc>${base}${x}</loc></url>`).join('')}</urlset>`;return new Response(xml,{headers:{'Content-Type':'application/xml'}})}
